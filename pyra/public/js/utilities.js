// create a new pop up window with url as its contentfunction popitup(url) {	newwindow=window.open(url,'name','height=600,width=600,location=no,scrollbars=1');	if (window.focus) {newwindow.focus()}	return false;}// removes all current options in a select box of a formfunction removeAllOptionsInSelectBox(selectbox){	//alert("here"+selectbox.name);	var i;	for(i=selectbox.options.length-1;i>=0;i--)	{		//selectbox.options.remove(i);		selectbox.remove(i);	}}// adds an option to the select box in a formfunction addOptionToSelectBox(selectbox, value, text ){	var optn = document.createElement("OPTION");	optn.text = text;	optn.value = value;	selectbox.options.add(optn);}function toggleBox(szDivID, iState) // 1 visible, 0 hidden{   var obj = document.layers ? document.layers[szDivID] :   document.getElementById ?  document.getElementById(szDivID).style :   document.all[szDivID].style;   obj.visibility = document.layers ? (iState ? "show" : "hide") :   (iState ? "visible" : "hidden");}